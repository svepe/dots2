-- indent-blankline — static indentation guide lines at every level. The config
-- module is named "ibl" (v3).
vim.pack.add({ { src = "https://github.com/lukas-reineke/indent-blankline.nvim" } })
require("ibl").setup()
